---
kind: pipeline
name: default

trigger:
  branch:
  - master
  event:
  - push

steps:
- name: build
  image: mhart/alpine-node:12
  pull: always
  commands:
  - npm i
  - npm run build
- name: deploy
  image: cschlosser/drone-ftps
  environment:
    repo: vito2005/traider
    FTP_USERNAME:
      from_secret: ftp_username
    FTP_PASSWORD:
      from_secret: ftp_password
    PLUGIN_HOSTNAME: ftp.h001273707.nichost.ru
    PLUGIN_VERIFY: false
    PLUGIN_CLEAN_DIR: true
    PLUGIN_DEST_DIR: /sukharev-trader.ru/docs
    PLUGIN_SRC_DIR: /dist

---
kind: signature
hmac: 466ec2ec05acda08a793f19391754d02f57e5f0622a8e5015f652958ed157323

...
