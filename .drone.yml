---
kind: pipeline
name: default

trigger:
  branch:
  - master
  event:
  - push

steps:
- name: build
  image: mhart/alpine-node:12
  pull: always
  commands:
  - npm i
  - npm run build
- name: deploy
  image: cschlosser/drone-ftps
  environment:
    repo: vito2005/traider
    FTP_USERNAME:
      from_secret: ftp_username
    FTP_PASSWORD:
      from_secret: ftp_password
    PLUGIN_HOSTNAME: ftp.h001273707.nichost.ru
    PLUGIN_VERIFY: false
    PLUGIN_CLEAN_DIR: true
    PLUGIN_DEST_DIR: /sukharev-trader.ru/docs
    PLUGIN_SRC_DIR: /dist

---
kind: signature
hmac: abb48f04e65a2e71c0dec71c177d7c6bfb444945adecd1e46e4e4d172708d5a6

...
